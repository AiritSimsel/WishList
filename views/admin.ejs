<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="box" id="heading">
        <h1>WishList Admin <%=date %></h1>
    </div>

    <div class="box">
        <% for(let i = 0; i < myWish.length; i++) { %>
            <form action="/delete" method="POST">
                <div class="item">
                    <input type="checkbox" value="<%= myWish[i].id %>" name="checkbox" onchange="this.form.submit()" >
                    <p><%= myWish[i].item %></p>
                </div>
            </form>
        <% } %>

    </div>
    <div class="box">
        <form action="/form" class="class" method="post">
            <input type="text" name="newWish" placeholder="Add New Wish">
            <button type="submit">+</button>
        </form>
    </div>
    
</body>
</html>
